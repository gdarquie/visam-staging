{% extends 'base.html.twig' %}

{% block body %}

    <section class="container">
        <h1>Créer un nouvel item thésaurus</h1>

        {{ form_start(edit_form) }}
            {{ include('EditeurBundle:Thesaurus:_form.html.twig') }}
            <input type="submit" value="Créer"  class="btn btn-hesam" formnovalidate/>
        {{ form_end(edit_form) }}

        <ul>
            <li>
                <a href="{{ path('editeur') }}">Retour</a>
            </li>
        </ul>
    </section>
{% endblock %}
