{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
    <h1>Editer une formation</h1>

    {{ form_start(edit_form) }}

        {{ include('editeur/formation/_form.html.twig') }}

        <input type="submit" class="btn red darken-1" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('search') }}">Back to the list</a>
        </li>
{#         <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li> #}
    </ul>
    </div>
{% endblock %}
